import { updatePassword } from "./actions";

export default async function UpdatePasswordPage({searchParams}:{searchParams:Promise<{error?:string}>}){const params=await searchParams;return <main className="authPage"><section className="authCard"><div className="brand"><span>G</span>GameDay Softball</div><h1>Choose a new password</h1><p className="muted">Use at least 12 characters and avoid passwords used on other sites.</p>{params.error&&<p className="error" role="alert">{params.error}</p>}<form action={updatePassword} className="form"><label>New password<input name="password" type="password" minLength={12} autoComplete="new-password" required /></label><label>Confirm password<input name="confirmation" type="password" minLength={12} autoComplete="new-password" required /></label><button className="button primary">Update password</button></form></section></main>}
